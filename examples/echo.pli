; echo program

(module main)

(require "stdlib/std.pli" io (bind!))

(define main
   (do!
     [print! "echo program"]
     [print! "to terminate, press ^C"]
     [letrec
       ([go!
          (lambda ()
            (do!
              [io/bind! (read!) print!]
              [go!]))])
       (go!)]))
