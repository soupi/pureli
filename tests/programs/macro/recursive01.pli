(module main)

(defmacro cond
  [() (error "*** error: cond - non-exhuastive patterns")]
  [(clause1 &rest)
     (if (car clause1)
         (car (cdr clause1))
         (cond &rest))])

(define main
  (do!
    [print! x]))

(define x
  (cond '(#f (error "error"))
        '(#f 2)
        '(#t 3)))
