; echo program with macro test

(module main)

(require "stdlib/std.pli" std (bind!))

(define main
   (do!
     [print! "echo program"]
     [letrec
       ([go!
          (lambda ()
            (do!
              [std/bind! (read!) print!]
              [go!]))])
       (go!)]))
